import Pricing from "@/components/pricing";

export const metadata = {
	title: "Pricing | Techin - IT Solution & Technology Next JS Template",
	description: "A simple Next.js app using the App Router",
};

const index = () => {
	return (
		<>
			<Pricing />
		</>
	);
};

export default index;
